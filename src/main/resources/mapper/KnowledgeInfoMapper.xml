<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanhai.dingdingdemo.knowledge.mapper.KnowledgeInfoMapper">

    <!-- 根据角色列表查询经验库信息 -->
    <select id="selectListByRoles" resultType="com.quanhai.dingdingdemo.knowledge.model.pojo.KnowledgeInfo">
        SELECT * FROM knowledge_info
        <where>
            <if test="roleList != null and roleList.size() > 0">
                AND (
                <foreach collection="roleList" item="role" open="" separator=" OR " close="">
                    role LIKE CONCAT('%', #{role}, '%')
                </foreach>
                )
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 根据ID和角色列表查询经验库信息 -->
    <select id="selectByIdAndRoles" resultType="com.quanhai.dingdingdemo.knowledge.model.pojo.KnowledgeInfo">
        SELECT * FROM knowledge_info WHERE id = #{id}
        <if test="roleList != null and roleList.size() > 0">
            AND (
            <foreach collection="roleList" item="role" open="" separator=" OR " close="">
                role LIKE CONCAT('%', #{role}, '%')
            </foreach>
            )
        </if>
        LIMIT 1
    </select>
    
    <!-- 获取所有不重复的失效模式 -->
    <select id="selectDistinctFailureModes" resultType="java.lang.String">
        SELECT DISTINCT failure_mode FROM knowledge_info WHERE failure_mode IS NOT NULL AND failure_mode != '' ORDER BY failure_mode
    </select>
    
    <!-- 获取所有不重复的问题来源 -->
    <select id="selectDistinctIssueSources" resultType="java.lang.String">
        SELECT DISTINCT issue_source FROM knowledge_info WHERE issue_source IS NOT NULL AND issue_source != '' ORDER BY issue_source
    </select>
    
    <!-- 获取所有不重复的产品机型 -->
    <select id="selectDistinctProductModels" resultType="java.lang.String">
        SELECT DISTINCT product_model FROM knowledge_info WHERE product_model IS NOT NULL AND product_model != '' ORDER BY product_model
    </select>
    
    <!-- 获取所有不重复的产品分类 -->
    <select id="selectDistinctProductCategories" resultType="java.lang.String">
        SELECT DISTINCT product_category FROM knowledge_info WHERE product_category IS NOT NULL AND product_category != '' ORDER BY product_category
    </select>

</mapper>