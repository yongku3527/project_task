<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanhai.dingdingdemo.knowledge.mapper.CompletionStatusMapper">

    <!-- 根据用户ID查询完成状态列表 -->
    <select id="selectListByUserId" resultType="com.quanhai.dingdingdemo.knowledge.model.pojo.CompletionStatus">
        SELECT * FROM completion_status WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <!-- 根据用户ID和经验信息ID查询完成状态 -->
    <select id="selectByUserIdAndKnowledgeInfoId" resultType="com.quanhai.dingdingdemo.knowledge.model.pojo.CompletionStatus">
        SELECT * FROM completion_status WHERE user_id = #{userId} AND knowledge_info_id = #{knowledgeInfoId} LIMIT 1
    </select>

    <!-- 根据用户ID和完成状态查询完成状态列表 -->
    <select id="selectListByUserIdAndStatus" resultType="com.quanhai.dingdingdemo.knowledge.model.pojo.CompletionStatus">
        SELECT * FROM completion_status WHERE user_id = #{userId} AND completion_status = #{completionStatus} ORDER BY create_time DESC
    </select>

</mapper>