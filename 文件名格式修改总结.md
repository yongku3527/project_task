# 文件名格式修改总结

## 概述
已将上传文件的文件名格式修改为"编号-名称-日期-时间戳"格式。

## 后端修改

### 1. 创建文件名格式化工具类
- **文件**: `src/main/java/com/quanhai/dingdingdemo/file/utils/FileNameFormatter.java`
- **功能**: 
  - `generateFormattedFileName(String number, String name, String originalFilename)`: 生成"编号-名称-日期-时间戳.扩展名"格式
  - `generateTimeBasedFileName(String name, String originalFilename)`: 生成"时间戳-名称.扩展名"格式
  - `extractFileInfo(String formattedFileName)`: 从格式化文件名中提取信息

### 2. 修改MinioService
- **文件**: `src/main/java/com/quanhai/dingdingdemo/service/MinioService.java`
- **修改**:
  - 新增`uploadFileWithFormattedName`方法：使用格式化文件名上传
  - 新增`uploadFileWithTimeBasedName`方法：使用时间戳格式化文件名上传
  - 修改原有`uploadFile`方法：增加原始文件名记录

### 3. 修改MinioController
- **文件**: `src/main/java/com/quanhai/dingdingdemo/controller/MinioController.java`
- **新增接口**:
  - `POST /buckets/{bucketName}/files/upload-formatted`: 使用格式化文件名上传
  - `POST /buckets/{bucketName}/files/upload-timestamp`: 使用时间戳格式化文件名上传
  - 保留原有`POST /buckets/{bucketName}/files`接口

## 前端修改

### 修改CircuitBoardManager.vue
- **文件**: `project_task/src/views/CircuitBoardManager.vue`
- **修改的上传方法**:
  1. `handleSchematicUpload` (原理图上传)
  2. `handleSmtUpload` (SMT文件上传)
  3. `handleLedBoardPluginUpload` (灯板插件上传)
  4. `handleBoardUpload` (线路板文件上传)

- **主要变更**:
  - 从预签名URL直传改为调用新的格式化上传接口
  - 使用当前线路板的boardCode和boardName生成格式化文件名
  - 上传成功后显示格式化后的文件名

## 文件名格式示例

### 格式化文件名格式
- **格式**: `编号~名称~日期~时间戳.扩展名`
- **示例**: `PCB001~测试线路板~20251011~1760146535394.pdf`

**注意**: 使用波浪号(~)作为分隔符，避免与文件名中可能出现的连字符(-)冲突

### 时间戳文件名格式
- **格式**: `时间戳-名称.扩展名`
- **示例**: `20251011093535-测试线路板.pdf`

## 测试验证

已创建并运行测试类验证FileNameFormatter功能：
- ✅ 格式化文件名生成正常
- ✅ 时间戳文件名生成正常
- ✅ 文件信息提取功能正常

## 使用说明

1. **原理图/SMT/灯板插件上传**: 在编辑线路板时上传文件，系统会自动使用当前线路板的编号和名称生成格式化文件名
2. **线路板文件上传**: 同样使用当前线路板信息生成格式化文件名
3. **所有上传**: 都会显示格式化后的文件名，便于识别和管理

## Bug修复记录

**问题描述**: 文件名一直使用'UNKNOWN'和'原理图'等默认值

**根本原因**: 上传方法错误地使用了`boardDialog.form`中的数据，而实际的上传功能位于半成品和灯板插件的编辑对话框中，这些对话框有自己的表单数据结构

**修复方案**: 
- `handleSchematicUpload`方法：使用`semiProductDialog.form.semiProductCode`和`semiProductDialog.form.semiProductName`
- `handleSmtUpload`方法：使用`semiProductDialog.form.semiProductCode`和`semiProductDialog.form.semiProductName`  
- `handleLedBoardPluginUpload`方法：使用`ledBoardPluginDialog.form.ledBoardPluginCode`和`ledBoardPluginDialog.form.ledBoardPluginName`
- `handleBoardUpload`方法：保持不变，继续使用`boardDialog.form`（这是正确的）

**影响范围**: 所有文件上传功能现在都能正确使用对应对话框中的实际编码和名称信息

## 注意事项

- 文件名中的特殊字符会被清理（替换为下划线）
- 日期格式为yyyyMMdd，时间戳为毫秒级时间戳
- 保留了原始文件名信息用于显示和记录
- 确保对话框表单数据在文件上传前已正确填充